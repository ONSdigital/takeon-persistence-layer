apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "persistence.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "persistence.name" . }}
    helm.sh/chart: {{ include "persistence.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "persistence.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
